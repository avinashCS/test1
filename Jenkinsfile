pipeline {
   agent any

  

   stages {
     
       stage('Preparation') {
         steps {
            
                bat 'echo "Preparing"'
                     bat 'echo "$BRANCH_NAME"'
         }
          }
       stage('Build') {
         steps {
             bat 'echo "build"'
              }
          }
      }
      }
